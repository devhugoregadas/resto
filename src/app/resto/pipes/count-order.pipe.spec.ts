import { CountOrderPipe } from './count-order.pipe';

describe('CountOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new CountOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
